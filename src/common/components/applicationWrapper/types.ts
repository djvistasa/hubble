type IApplicationWrapperProps = { children: React.ReactNode };

type IStyledApplicationWrapperProps = {
  hasPadding?: boolean;
  fullWidth?: boolean;
};

export type { IApplicationWrapperProps, IStyledApplicationWrapperProps };
